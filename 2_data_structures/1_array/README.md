## Array
    - Collection of elements of the same data type stored in contiguous memery locations.
    - Java arrays can hold both primitive types and objects.
    - primitive types arrays, stored elements in contiguous locations and for Non-primitive references to items are stores in contiguous locations.